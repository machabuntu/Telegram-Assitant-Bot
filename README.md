# AI Assistant Bot

Telegram бот с семью основными функциями:
1. **📹 Анализ YouTube видео** - создание кратких содержаний
2. **🖼️ Анализ изображений** - описание изображений
3. **🎨 Генерация изображений** - создание изображений по описанию
4. **🔤 Генерация азбуки** - создание тематической русской азбуки
5. **✨ Изменение изображений** - модификация изображений
6. **🔄 Последовательное изменение** - цепочка изменений одного изображения
7. **💰 Проверка баланса** - мониторинг баланса OpenRouter

## Функционал

### 📹 Анализ YouTube видео:
- 📥 Скачивание аудио с YouTube видео с помощью yt-dlp
- 🎤 Транскрипция аудио через OpenAI Whisper
- 🤖 Создание краткого содержания через Grok API
- 📱 Отправка результата в Telegram

### 🖼️ Анализ изображений:
- 🔗 Анализ изображений по ссылке
- 📸 Анализ последнего изображения из чата
- 🤖 Описание изображений через OpenRouter/Grok API
- 📱 Отправка подробного описания в Telegram

### 🎨 Генерация изображений:
- ✨ Генерация изображений по текстовому описанию
- 🤖 Использование Google Gemini 2.5 Flash Image через OpenRouter
- 💾 Автоматическое сохранение в папку `generated_images/`
- 📱 Отправка изображения в Telegram

### 🔤 Генерация русской азбуки:
- 📝 Создание тематической азбуки по заданному описанию
- 🤖 Использование OpenAI GPT-5 Image Mini через OpenRouter
- 💾 Автоматическое сохранение в папку `generated_images/`
- 📱 Отправка азбуки в Telegram

### ✨ Изменение изображений:
- 🖼️ Изменение изображений из чата по запросу
- 🔄 Последовательное изменение сгенерированных изображений
- 💾 Сохранение всех версий изображений
- 📱 Отправка результата в Telegram

### 💰 Проверка баланса:
- 📊 Получение информации о балансе OpenRouter API
- 💳 Отображение общих кредитов и использования
- ✅ Автоматический расчет остатка средств
- 📱 Вывод детальной информации в Telegram

## Конфигурация

### Настройка API и моделей

Все команды бота настраиваются через файл `config.json`. Каждая команда может использовать разные API провайдеры и модели.

#### Структура конфигурации

```json
{
  "command_api": {
    "provider": "openrouter",
    "openrouter": {
      "url": "https://openrouter.ai/api/v1/chat/completions",
      "key": "your-openrouter-key",
      "model": "model-name"
    },
    "grok": {
      "url": "https://api.x.ai/v1/chat/completions",
      "key": "your-grok-key", 
      "model": "grok-model"
    }
  }
}
```

#### Доступные команды и их конфигурации

| Команда | API конфигурация | Провайдер по умолчанию | Модель по умолчанию |
|---------|------------------|------------------------|---------------------|
| `/summary` | `summary_api` | Grok | `grok-4-fast` |
| `/describe` | `describe_api` | OpenRouter | `openai/gpt-5` |
| `/imagegen` | `imagegen_api` | OpenRouter | `google/gemini-2.5-flash-image` |
| `/abcgen` | `abcgen_api` | OpenRouter | `openai/gpt-5-image` |
| `/imagechange` | `imagechange_api` | OpenRouter | `google/gemini-2.5-flash-image` |
| `/changelast` | `changelast_api` | OpenRouter | `google/gemini-2.5-flash-image` |
| `/mergeimage` | `mergeimage_api` | OpenRouter | `google/gemini-2.5-flash-image` |
| `/balance` | `balance_api` | OpenRouter | - |

#### Переключение провайдеров

Для смены провайдера измените значение `"provider"` в соответствующей секции:

```json
{
  "summary_api": {
    "provider": "grok",  // или "openrouter"
    "grok": { ... },
    "openrouter": { ... }
  }
}
```

## Установка

### 1. Подготовка виртуального окружения

```bash
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

Убедитесь, что у вас установлены:
- openai-whisper
- yt-dlp
- python-telegram-bot
- requests

### 2. Настройка конфигурации

Создайте файл `config.json` со следующим содержимым:

```json
{
    "telegram_token": "YOUR_TELEGRAM_BOT_TOKEN",
    "allowed_channel_id": "YOUR_CHANNEL_ID",
    "yt_dlp_path": "venv/Scripts",
    "whisper_path": "venv/Scripts",
    "youtube_cookies": "cookies.txt",
    "summary_api": {
        "url": "https://api.x.ai/v1/chat/completions",
        "key": "YOUR_GROK_API_KEY",
        "model": "grok-4-fast"
    },
    "describe_api": {
        "provider": "openrouter",
        "grok": {
            "url": "https://api.x.ai/v1/chat/completions",
            "key": "YOUR_GROK_API_KEY",
            "model": "grok-4-latest"
        },
        "openrouter": {
            "url": "https://openrouter.ai/api/v1/chat/completions",
            "key": "YOUR_OPENROUTER_API_KEY",
            "model": "google/gemini-2.5-flash-image"
        }
    }
}
```

### 3. Получение необходимых токенов

#### Telegram Bot Token:
1. Напишите @BotFather в Telegram
2. Используйте команду `/newbot`
3. Следуйте инструкциям и скопируйте токен

#### Grok API Key:
1. Перейдите на https://console.x.ai/
2. Зарегистрируйтесь и создайте API ключ

#### OpenRouter API Key:
1. Перейдите на https://openrouter.ai/
2. Зарегистрируйтесь и создайте API ключ

## 🚀 Запуск бота на Windows

### Быстрый старт (одним кликом)

#### Вариант 1: Запуск с видимым окном консоли
Просто дважды кликните по файлу `start_bot.bat` в проводнике Windows.

#### Вариант 2: Скрытый запуск (без окна консоли)
Дважды кликните по файлу `start_bot_hidden.vbs` - бот запустится в фоновом режиме.

### 📋 Доступные скрипты

| Файл | Описание |
|------|----------|
| `start_bot.bat` | Запуск бота с видимым окном консоли |
| `start_bot_hidden.vbs` | Скрытый запуск без окна консоли |
| `stop_bot.bat` | Остановка бота |
| `status_bot.bat` | Проверка статуса бота (запущен/остановлен) |
| `add_to_startup.bat` | Добавить бот в автозагрузку Windows |
| `remove_from_startup.bat` | Удалить бот из автозагрузки |

### ⚙️ Добавление в автозагрузку Windows

1. **Запустите `add_to_startup.bat`** (правой кнопкой → Запуск от имени администратора)
   - Бот будет автоматически запускаться при входе в Windows
   - Работает в фоновом режиме без видимого окна
   - Ярлык добавляется в папку автозагрузки Windows

2. **Для удаления из автозагрузки** запустите `remove_from_startup.bat`

### 🔍 Проверка статуса бота

Запустите `status_bot.bat` чтобы узнать:
- Запущен ли бот сейчас
- PID процесса (если запущен)
- Включена ли автозагрузка

## 📝 Команды бота

### Основные команды:

#### 📹 Анализ YouTube видео:
```
/summary https://www.youtube.com/watch?v=VIDEO_ID
```

#### 🖼️ Анализ изображений:
```
/describe                                    # Анализ последнего изображения в чате
/describe https://example.com/image.jpg     # Анализ изображения по ссылке
```

#### 🎨 Генерация изображений:
```
/imagegen кот в космосе среди звёзд
```

#### 🔤 Генерация русской азбуки:
```
/abcgen космос                        # Создаёт русскую азбуку на тему космоса
/abcgen животные                      # Создаёт русскую азбуку с животными
/abcgen фрукты и овощи                # Каждая буква представлена фруктом или овощем
```

#### ✨ Изменение изображений:
```
/imagechange сделай изображение в стиле аниме    # Изменяет последнее изображение из чата
/changelast добавь неоновое освещение            # Изменяет последнее сгенерированное изображение
```

#### 🔀 Объединение изображений:
```
# Отправьте несколько изображений одним сообщением, затем:
/mergeimage объедини эти изображения в одно
/mergeimage сравни эти фотографии
/mergeimage найди отличия между изображениями
```

#### 💰 Проверка баланса:
```
/balance    # Показывает баланс OpenRouter API
```

### Примеры использования:

#### Последовательное изменение изображения:
```
1. /imagegen красивый закат над морем
2. /changelast добавь пальмы на переднем плане
3. /changelast сделай небо более драматичным
4. /changelast добавь силуэт лодки
```

Каждый шаг автоматически сохраняется в папку `generated_images/`!

## 📁 Структура проекта

```
F:\OpenAI-Whisper\
├── ai_assistant_bot.py          # Основной файл бота
├── config.json                   # Конфигурация (не коммитить!)
├── requirements.txt              # Python зависимости
├── README.md                     # Документация
│
├── start_bot.bat                 # 🚀 Запуск с окном консоли
├── start_bot_hidden.vbs          # 🚀 Скрытый запуск
├── stop_bot.bat                  # ⛔ Остановка бота
├── status_bot.bat                # 📊 Проверка статуса
├── add_to_startup.bat            # ⚙️ Добавить в автозагрузку
├── remove_from_startup.bat       # ⚙️ Удалить из автозагрузки
│
├── generated_images/             # 💾 Сгенерированные изображения
│   ├── README.md
│   ├── imagegen_*.png
│   ├── imagechange_*.png
│   └── changelast_*.png
│
└── venv/                         # Виртуальное окружение
    └── Scripts/
        ├── yt-dlp.exe
        └── whisper.exe
```

## 🔧 Устранение неполадок

### Бот не запускается

1. **Проверьте виртуальное окружение:**
   ```bash
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```

2. **Проверьте config.json:**
   - Файл должен существовать
   - Все ключи API должны быть заполнены
   - Формат JSON должен быть корректным

3. **Проверьте статус:**
   - Запустите `status_bot.bat`

### Ошибка "Файл конфигурации не найден"
- Создайте `config.json` в папке с ботом
- Заполните все необходимые параметры

### Ошибка при генерации изображений
- Проверьте OpenRouter API ключ
- Убедитесь, что на счету достаточно средств
- Проверьте доступность API

### Бот запущен, но не отвечает
- Проверьте `allowed_channel_id` в конфигурации
- Убедитесь, что бот добавлен в канал как администратор

## 🔒 Безопасность

- ✅ Бот работает только с указанными каналами
- ✅ Временные файлы автоматически удаляются
- ✅ API ключи хранятся локально в `config.json`
- ⚠️ **Не коммитьте `config.json` в Git!**
- ✅ `.gitignore` уже настроен для защиты конфиденциальных данных

## 📊 Логирование

Бот ведет подробные логи всех операций:
- Получение команд
- Процесс обработки
- Генерация и изменение изображений
- Ошибки и предупреждения

Логи отображаются в консоли (если бот запущен через `start_bot.bat`)

## 📦 Зависимости

- Python 3.8+
- python-telegram-bot
- requests
- openai-whisper
- yt-dlp

## 🎯 Советы по использованию

1. **Для автозапуска при входе в Windows:** используйте `add_to_startup.bat`
2. **Для работы в фоне:** используйте `start_bot_hidden.vbs`
3. **Для последовательных изменений:** используйте `/changelast` вместо копирования изображений
4. **Все изображения сохраняются** в `generated_images/` с временными метками

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи бота в консоли
2. Статус бота через `status_bot.bat`
3. Правильность заполнения `config.json`
4. Доступность API сервисов

---

**Версия:** 2.0  
**Последнее обновление:** 2025-10-14
