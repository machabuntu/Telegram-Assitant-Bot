# 🚀 Быстрый старт - AI Assistant Bot

## Запуск бота одним кликом в Windows

### 📁 Доступные файлы для запуска:

| Двойной клик по файлу | Что произойдет |
|----------------------|----------------|
| **start_bot.bat** | ✅ Бот запустится с видимым окном консоли (удобно для отладки) |
| **start_bot_hidden.vbs** | ✅ Бот запустится скрыто в фоне (без окна) |
| **stop_bot.bat** | ⛔ Остановит работающего бота |
| **status_bot.bat** | 📊 Покажет статус: запущен ли бот, включена ли автозагрузка |
| **add_to_startup.bat** | ⚙️ Добавит бота в автозагрузку Windows |
| **remove_from_startup.bat** | ⚙️ Удалит бота из автозагрузки |

---

## 🎯 Сценарии использования:

### Сценарий 1: Запустить бота прямо сейчас
1. Дважды кликните **start_bot.bat**
2. Откроется окно консоли с логами
3. Для остановки нажмите `Ctrl+C` или закройте окно

### Сценарий 2: Запустить бота в фоне (без окна)
1. Дважды кликните **start_bot_hidden.vbs**
2. Бот запустится незаметно в фоне
3. Для остановки запустите **stop_bot.bat**

### Сценарий 3: Добавить в автозагрузку Windows
1. Дважды кликните **add_to_startup.bat**
2. Подтвердите действие
3. Теперь бот будет запускаться автоматически при входе в Windows!
4. Бот будет работать скрыто в фоновом режиме

### Сценарий 4: Проверить, работает ли бот
1. Дважды кликните **status_bot.bat**
2. Увидите:
   - Запущен ли бот (и его PID)
   - Включена ли автозагрузка

---

## ⚠️ Перед первым запуском:

### 1. Убедитесь, что установлены зависимости:
```bash
# Откройте PowerShell или CMD в папке проекта
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Создайте файл config.json с вашими API ключами:
```json
{
    "telegram_token": "ВАШ_TELEGRAM_TOKEN",
    "allowed_channel_id": ["ВАШ_CHANNEL_ID_1", "ВАШ_CHANNEL_ID_2"],
    "yt_dlp_path": "venv/Scripts",
    "whisper_path": "venv/Scripts",
    "youtube_cookies": "cookies.txt",
    "summary_api": {
        "url": "https://api.x.ai/v1/chat/completions",
        "key": "ВАШ_GROK_API_KEY",
        "model": "grok-4-fast"
    },
    "describe_api": {
        "provider": "openrouter",
        "grok": {
            "url": "https://api.x.ai/v1/chat/completions",
            "key": "ВАШ_GROK_API_KEY",
            "model": "grok-4-latest"
        },
        "openrouter": {
            "url": "https://openrouter.ai/api/v1/chat/completions",
            "key": "ВАШ_OPENROUTER_API_KEY",
            "model": "google/gemini-2.5-flash-image"
        }
    }
}
```

---

## 📋 Команды бота в Telegram:

### Генерация и изменение изображений:
```
/imagegen кот в космосе
/imagechange сделай в стиле аниме
/changelast добавь больше звёзд
```

### Анализ контента:
```
/summary https://youtube.com/watch?v=VIDEO_ID
/describe (анализ последнего изображения в чате)
```

---

## 🔧 Решение проблем:

### Бот не запускается?
1. Запустите **start_bot.bat** чтобы увидеть ошибку
2. Проверьте, что файл `config.json` существует и заполнен
3. Проверьте, что виртуальное окружение создано и активировано

### Как остановить бота?
- Если запущен через **start_bot.bat**: нажмите `Ctrl+C` или закройте окно
- Если запущен через **start_bot_hidden.vbs**: запустите **stop_bot.bat**

### Как узнать, работает ли бот?
- Запустите **status_bot.bat**

### Как убрать из автозагрузки?
- Запустите **remove_from_startup.bat**

---

## 💡 Полезные советы:

1. **Для работы в фоне 24/7**: добавьте бота в автозагрузку через **add_to_startup.bat**
2. **Для отладки**: используйте **start_bot.bat** чтобы видеть логи
3. **Все изображения сохраняются** в папке `generated_images/`
4. **Используйте /changelast** для последовательных изменений без копирования изображений

---

## 📞 Нужна помощь?

1. Проверьте полную документацию в **README.md**
2. Запустите **status_bot.bat** для диагностики
3. Откройте **start_bot.bat** чтобы увидеть логи ошибок

---

**Готово! Теперь вы можете запускать бота одним кликом!** 🎉


